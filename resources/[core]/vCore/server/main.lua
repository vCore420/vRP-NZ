-- Main server logic for vRP NZ Core

vCore = {}

AddEventHandler('playerConnecting', function(name, setKickReason, deferrals)
    -- Placeholder: handle player connecting, identity, etc.
    print(('[vCore] Player connecting: %s'):format(name))
end)